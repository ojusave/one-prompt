# See README for full documentation

# Database (live mode only — replay works without this)
DATABASE_URL=

# Live mode (optional, feature-flagged)
LIVE_MODE_ENABLED=false
DEMO_SCRIPTED_MODE=true
PRESENTER_TOKEN=

# Public config
NEXT_PUBLIC_GITHUB_REPO_URL=https://github.com/render-examples/one-prompt

# Render Workflow (configure in Dashboard)
RENDER_WORKFLOW_SERVICE_ID=
